language: python
cache: pip
python:
  - "2.7"
# command to install dependencies
install: "pip install -r .gitlab-ci.d/requirements.txt"
# command to run tests
script:
  - cd ..
  - wget https://github.com/DIRACGrid/DIRAC/archive/integration.zip
  - unzip -q integration.zip
  - mv DIRAC-integration DIRAC
  - export DIRAC=${PWD}
  - cd ILCDIRAC
  - export PYTHONPATH=${PWD%/*}
  - echo $PYTHONPATH
  - .gitlab-ci.d/run-unittests.sh
after_success:
  - codecov
